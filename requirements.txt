# Vercel / serverless — minimal runtime (~120–180 MB installed)
# Local full stack: pip install -r requirements-full.txt

flask==3.0.3
python-dotenv==1.0.1
nltk==3.8.1
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.4.2
joblib==1.3.2
threadpoolctl==3.5.0

# Lightweight URL extraction (no newspaper3k)
requests==2.32.3
beautifulsoup4==4.12.3

# Optional: language detection only (translation disabled without deep-translator)
langdetect==1.0.9
